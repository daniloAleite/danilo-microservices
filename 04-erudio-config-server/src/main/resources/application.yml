server:
 port: 8888
spring:
 application:
   name: erudio-config-server
 cloud:
  config:
    server:
      git:
        uri: https://github.com/daniloAleite/test-cloud-config-server
        #usuário: usuário
        #senha: senha
        default-label: master
        search-paths:
        - 'greeting-service'